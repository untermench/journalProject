<Window x:Class="journalProject.AdminWins.LessonItemAddWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:journalProject.AdminWins"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen" WindowStyle="None" 
        Background="Transparent" AllowsTransparency="True"
        ResizeMode="CanResize" WindowState="Normal" 
        Title="GroupSelectTeacher" Height="45" Width="262">
    <Border Background="White" CornerRadius="8">
        <Grid Background="Transparent">

            <Button x:Name="AddLessonItem" Style="{DynamicResource AddButtonMini}" Height="26" Width="25" Content="Удалить" HorizontalAlignment="Left" Margin="207,0,0,0" VerticalAlignment="Center" Click="AddLessonItem_Click"/>
            <Button x:Name="btnClose" Style="{StaticResource IconButtonsStyle}" Content="{DynamicResource close}" Click="btnClose_Click" Tag="IsCloseButton" Cursor="Hand" Margin="237,0,0,0" VerticalAlignment="Top"/>
            <TextBox Style="{DynamicResource SearchBox}" x:Name="LessonItemNameBox" HorizontalAlignment="Left" Margin="10,15,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="175"/>

        </Grid>
    </Border>
</Window>
